# Ena Build System

# Not working, just a proposal

# used for user configuration, called somewhere internally
ena.build {
    "./src/*.ena" ena.include
}

ena.deps {
    ena.dep.std
    "kislball/some_library@git_tag" ena.dep.github
}

main {
    ena.build.main
}